{
  "name": "avaast",
  "private": true,
  "description": "AVaaSt - App View as a Service(t) for AT Protocol",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
